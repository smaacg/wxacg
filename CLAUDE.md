# Claude Code 專案開發規範

## 1. 全域語言規範

* 所有回覆、分析、計畫、任務說明與建議，使用台灣繁體中文。
* 程式碼註解優先使用繁體中文。
* 技術名詞、API 名稱、函式名稱、變數名稱、套件名稱、指令與程式碼本身可保留原文。
* 不得無故使用簡體中文。
* 回覆應簡潔、直接、清楚。

---

# 2. 修改前確認

在實際修改、新增或刪除任何檔案之前：

1. 先理解相關專案架構。
2. 查看相關原始碼。
3. 如果是 Bug，先查看相關 Log、錯誤訊息與設定。
4. 說明問題判斷。
5. 提出修改方案。
6. 列出預計修改的檔案。
7. 說明可能影響。
8. 等使用者明確同意後才能修改檔案。

未取得使用者明確同意前，不得：

* 修改程式碼
* 新增檔案
* 刪除檔案
* 重構架構

---

# 3. 最小範圍修改

* 只修改與目前任務直接相關的程式碼。
* 不得順便修改無關程式。
* 不得任意重新排版無關程式碼。
* 不得任意重新命名既有變數、函式、類別或檔案。
* 不得擅自改變既有資料流程。
* 不得擅自修改無關設定。

---

# 4. 禁止擅自重構

一般維護、Bug 修正或增加功能時：

* 不得擅自拆分模組。
* 不得擅自合併模組。
* 不得擅自更換框架。
* 不得擅自更換核心技術方案。
* 不得因為其他架構看起來比較漂亮而改寫現有架構。

如認為必須重構：

1. 說明原因。
2. 說明影響。
3. 提出方案。
4. 等使用者明確確認。

---

# 5. 註解保留

* 嚴禁無故刪除原有中文註解。
* 必須保留原有 Docblocks 與行內註解。
* 不得為了縮短程式碼而刪除註解。
* 如果原有註解與程式邏輯不一致，先告知使用者。
* 不得默默刪除或改寫重要註解。

---

# 6. Debug 規範

遇到 Error、Exception、Warning、Fatal Error 或異常行為時，必須先調查。

優先查看：

1. 錯誤訊息
2. Log
3. 相關原始碼
4. 相關設定
5. 錯誤前後資料流程
6. API Request / Response
7. 必要的外部文件

不得僅根據錯誤訊息表面文字直接修改。

如果證據不足：

* 明確告知目前只是推測。
* 不得把推測說成確定原因。
* 繼續調查相關證據。

---

# 7. 禁止吞掉錯誤

不得為了讓測試通過而：

* 使用 `except: pass`
* 使用無意義的 `try-except`
* 靜默忽略 Exception
* 靜默忽略 Warning
* 隱藏 API 錯誤
* 將錯誤轉換成假成功

錯誤必須保留適當的資訊並進行追蹤。

---

# 8. API / 資料介面

修改以下內容時：

* AJAX Payload
* REST API
* API Request
* API Response
* JSON
* Request Parameters
* Response Parameters
* 資料表欄位
* 前端資料模型
* 後端資料模型

必須同時檢查：

【傳送端】
↓
【資料格式】
↓
【API】
↓
【接收端】
↓
【資料處理】
↓
【資料儲存】

不得只修改單端。

---

# 9. 第三方依賴

除非確實必要：

* 不得任意新增第三方套件。
* 不得任意下載陌生套件。
* 優先使用現有依賴。
* 優先使用原生功能。

如果需要新增套件，先說明：

1. 為什麼現有工具無法完成。
2. 套件名稱。
3. 用途。
4. 維護與安全風險。

等待使用者確認後再安裝。

---

# 10. Git / Commit

## 禁止自動 Push

修改程式後：

* 不得自行執行 `git push`。
* 不得自行部署 Production。
* 不得自行發布正式版本。

只有使用者明確授權，例如：

* 「確認 OK」
* 「可以 Push」
* 「可以部署」
* 「請幫我 Push」
* 「確認可以上線」

才能執行。

---

## Commit 前

執行 `git commit` 前：

1. 確認檔案已存檔。
2. 執行 `git status`。
3. 查看 `git diff`。
4. 確認沒有無關修改。
5. 確認沒有敏感資訊。
6. 確認相關測試結果。

---

## Commit 訊息

使用 Conventional Commits：

* `feat:`
* `fix:`
* `refactor:`
* `chore:`
* `docs:`
* `test:`
* `perf:`

Commit 訊息使用繁體中文描述實際修改。

例如：

`feat: 新增 WordPress 標籤快取機制`

---

# 11. 部署

嚴禁自動部署。

流程：

【修改】
↓
【本地測試】
↓
【使用者確認】
↓
【明確授權】
↓
【Commit】
↓
【Push / 部署】

未取得明確授權前，不得：

* `git push`
* `gcloud run deploy`
* Cloud Run 部署
* WordPress Production 部署
* Production 發布
* 任何正式環境修改

---

# 12. 破壞性操作

以下操作執行前必須取得使用者明確確認：

* `git reset --hard`
* `git clean`
* `rm -rf`
* 大量刪除檔案
* 覆蓋重要檔案
* DROP TABLE
* 大量刪除資料
* Production 資料修改
* 任何不可逆資料操作

執行前必須說明：

* 會做什麼
* 哪些資料會受到影響
* 是否可以復原

---

# 13. 測試

修改完成後：

1. 執行相關測試。
2. 檢查語法錯誤。
3. 檢查 Import / Dependency。
4. 檢查 API Request / Response。
5. 檢查相關 Log。
6. 測試失敗時不得忽略。
7. 回報測試結果。

如果無法測試，必須說明原因。

不得宣稱沒有實際執行的測試「已通過」。

---

# 14. 標準工作流程

一般任務：

【理解需求】
↓
【分析程式】
↓
【查看證據】
↓
【提出方案】
↓
【等待確認】
↓
【修改】
↓
【測試】
↓
【回報】
↓
【等待使用者確認】
↓
【Commit / Push / 部署】

除非使用者明確要求直接執行，不得跳過確認流程。

---

# 15. 核心原則

優先：

1. 正確性
2. 安全性
3. 可維護性
4. 最小修改
5. 保持既有架構
6. 正確錯誤處理
7. 可追蹤 Git 歷史

不得為了速度、漂亮的程式碼或暫時通過測試而犧牲以上原則。
